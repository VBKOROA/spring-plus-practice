meta {
  name: todo 검색
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/todos/search
  body: json
  auth: inherit
}

headers {
  Authorization: {{bearer}}
}

body:json {
  {
    "title": "o"
  }
}

settings {
  encodeUrl: true
}
